<main>
    <h2 class="page-name">NEW BAND!</h2>
    <form action="<%= bands_url %>" method="post" class="flex-column">
        <%= auth_token %>
        <div class="flex">
            <label for="name">Name</label>
            <input type="text" id="name" name="band[name]">
        </div>
        <input type="submit" value="Submit Query" class="button submit">
    </form>
</main>